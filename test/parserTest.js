'use strict';

const should = require('chai').should();




describe('mt940js adapter tests', () => {
	it('have a read method', () => {
		const f = typeof parser.read;
		f.should.equal('function');
	});
});
